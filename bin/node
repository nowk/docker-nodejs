#!/bin/bash
# vim: set filetype=sh :
set -e

VERSION=5.12.0
DOCKER_IMAGE="node:$VERSION"

# include _docker.sh
. "$(dirname $0)/../lib/docker.sh/_docker.sh"

docker $(run_with --volumes-from npmv${VERSION} \
	-v=$(pwd):/usr/src \
	-w=/usr/src \
	--entrypoint node)

